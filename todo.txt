-- ui --
* text input
* wrapping text
* depth of field effect
* style switch based on #element.type & #element.class
* fix weird highlighting issue in nau demo

-- language --
* map "foo as string" -> foo.asString()
* allow local var to conflict with global var
* heap local to stop most of intrafunction refcount'ing
* property

-- property --
foo : 'i32 { get; }	// must be set during init
foo : 1 {
	get { } : 1 or := 1
}
foo := 1 {
	get { } set { _ // new }
}
foo := 0 {
	changed { _ // old, _foo // new } 
}
