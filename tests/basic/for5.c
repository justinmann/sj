#include <stdio.h>
#include <stdlib.h>

int main() {
    int loopEnd1;
    int x;
    int y;
    y = 0;
    x = 2;
    loopEnd1 = 4;
    while (x <= loopEnd1) {
        y = (y) + (x);
        x++;
    }
    y;
    return 0;
}
