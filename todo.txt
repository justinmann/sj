-- ui --
* text input
* wrapping text
* depth of field effect
* style switch based on #element.type & #element.class
* fix weird highlighting issue in nau demo

-- language --
* heap local to stop most of intrafunction refcount'ing
* property
* clone operator to do full copy
* syntax highlighting using textmate
* make sure debug out is compilable

-- property --
foo : 'i32 { get; }	// must be set during init
foo : 1 {
	get { } : 1 or := 1
}
foo := 1 {
	get { } set { _ // new }
}
foo := 0 {
	changed { _ // old, _foo // new } 
}

-- notes --
* fix mac build:
brew install llvm && rm /usr/local/opt/emscripten/libexec/llvm/bin/llvm-ar && ln -s /usr/local/opt/llvm/bin/llvm-ar /usr/local/opt/emscripten/libexec/llvm/bin/llvm-ar